# CanvasKit

CanvasKit is a TypeScript-first engine for interactive visual editors on an infinite canvas.

## Phase 1

Phase 1 provides rectangle scenes, Canvas 2D rendering, pan/zoom navigation, pointer events, and versioned JSON persistence. See [Getting Started](docs/getting-started.md) or run `pnpm --filter @canvaskit/basic-canvas dev`.
