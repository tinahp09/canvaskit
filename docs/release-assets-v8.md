# CanvasKit v8.0.0 release asset manifest

Release evidence is captured from `examples/editor-session` and committed in
`docs/public/releases/v8/`:

- `v8.0-overview.png`: local-first editor workspace with recovery controls.
- `v8.0-recents.png`: saved manifest and visible ordered recents.
- `v8.0-restored.png`: restored previous workspace with active tab.
- `v8.0-workspace-recovery.gif`: a 5–15 second sequence showing select tab →
  save workspace → switch tab → restore previous workspace.

The same workflow is covered by committed Playwright E2E tests.
