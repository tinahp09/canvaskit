# CanvasKit v7.0.0 release asset manifest

Release evidence is captured from `examples/editor-session` and committed in
`docs/public/releases/v7/`:

- `v7.0-overview.png`: restored multi-document glass workspace.
- `v7.0-save-status.png`: active document during and after host-adapter save.
- `v7.0-retry.png`: visible adapter error followed by retry readiness.
- `v7.0-persistence.gif`: a 5–15 second sequence showing add → save → restore
  → simulated failure → retry.

The same workflow is covered by committed Playwright E2E tests.
