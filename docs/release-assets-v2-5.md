# V2.5 release asset manifest

V2.5 has verified core, renderer, build, and browser evidence. Binary media is
not tracked because this repository has no approved release-media location.

Planned captures: `v2.5-assets-demo.gif` (add image asset → place image node →
export SVG → undo), plus overview, selected image node, and exported SVG views.

Automated evidence: asset/reference/migration tests; Canvas and SVG renderer
tests; basic-canvas browser flow for adding and undoing an image node. External
publishing remains approval-gated.
