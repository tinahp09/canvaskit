# V2.4 release asset manifest

V2.4 has verified source and browser evidence. Binary GIF/screenshots are not
tracked because this repository has no approved release-media location.

Planned captures: `v2.4-smart-layout-demo.gif` (snap → guide → grid layout →
undo), overview, active-guide overlay, grid result, and keyboard controls.

Automated evidence: Core guide/migration/layout tests; Canvas overlay test;
basic-canvas browser test for guide history and auto-layout undo; full unit and
browser suites at feature freeze. External publishing remains approval-gated.
