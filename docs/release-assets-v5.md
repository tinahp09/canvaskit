# CanvasKit v5.0.0 release asset manifest

Release evidence is captured from `examples/collaboration-adapters` and
committed in `docs/public/releases/v5/`:

- `v5.0-overview.png`: two open browser peers.
- `v5.0-remote-sync.png`: Ada creates a rectangle and Bea receives it.
- `v5.0-unavailable.png`: unavailable BroadcastChannel with untouched scenes.
- `v5.0-collaboration-adapters.gif`: six-second overview → sync → failure
  sequence, captured from those real browser states.

The interaction is also covered by the committed Playwright E2E suite.
